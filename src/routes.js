const router = require( 'express' ).Router()

function setRoutes () {
    router.post( '/anonimize-dicom', ( req, res ) => {

    })
    
    router.post( '/deanonimize-dicom', ( req, res ) => {
    
    })
}

module.exports = setRoutes
